image:
  pullPolicy: IfNotPresent
  repository: plexripper/plexripper
  tag: 0.26.0@sha256:4711fca69eb15f1e1df07c6930f36e76542633bc54bd37a71c6cbe603245124a
service:
  main:
    ports:
      main:
        protocol: http
        targetPort: 7000
        port: 7000
workload:
  main:
    podSpec:
      containers:
        main:
          probes:
            liveness:
              enabled: true
              type: http
            readiness:
              enabled: true
              type: http
            startup:
              enabled: true
              type: http
persistence:
  config:
    enabled: true
    mountPath: /Config
  downloads:
    enabled: true
    mountPath: /Downloads
portal:
  open:
    enabled: true
